import s from "./Text.module.css";

const Text = () => {
  return (
    <p className={s.text}>
      Each page, each chapter is a new round of knowledge, a new step towards
      understanding. By rewriting statistics, we create our own reading history.
    </p>
  );
};

export default Text;
